#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR="$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &>/dev/null && pwd )"

command -v uv >/dev/null \
  ||  { echo "ERROR: uv not found in PATH"; exit 1; }

cd "$SCRIPT_DIR"
uv sync --quiet --frozen

exec uv run python -m gh_tt "$@"